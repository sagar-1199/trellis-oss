---
title: Example App
type: project
path: /Users/you/code/example-app   # ABSOLUTE path to the real repo on your disk
trust: standard                     # safe | standard | high
verify: "npm run build && npm run test"
status: active
tags: [web, api]
related: ["[[Example Concept]]"]
updated: 2026-01-01
---

# Example App

> Replace this whole file with a real project. One page per repo you work on.

A one-paragraph description of what this project is and does.

- **Repo:** `/Users/you/code/example-app` (git, remote `github.com/you/example-app`).
- **Stack:** the frameworks/languages, in one line.
- **Run:** the exact dev command(s).
- **Verify:** `npm run build && npm run test` (the loop's gate).

## Trust: standard
Why this tier. `high` = production/PII/payments → PR-only, hands-off paths
(`auth/`, `migrations/`, `.env*`, secrets are always off-limits regardless).

## Decisions
- Durable choices and **why** — the reasoning future-you (and the agent) needs.

## Gotchas
- The non-obvious traps. Add one line whenever something bites you.

## Current focus
- What's being worked on right now.
